#include <iostream>
using namespace std;

int main() {
	int number, f = 2;
	cout << "enter a number : "; cin >> number;
	cout << "prime factorization : ";

	while (number > 1) {
		while (number % f == 0) {
			cout << f << " ";
			number /= f;
		}
		f++;
	}
	cout << endl;
	return 0;
}
